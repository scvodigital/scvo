{{> search_info paging=@root.data.primaryResponse.pagination}}

{{#each @root.data.primaryResponse.hits.hits ~}}
<div class="card search-result {{_type}} {{_type}}-{{_id}} b-margin-full">
  {{#with _source ~}}
  <div class="card-header">
    <div class="card-title h4">
      {{#ifAny path url ~}}
      <a href="{{default path url}}">{{{truncate (trim title) 140}}}</a>
      {{else}}
      {{#compare ../_type "goodmoves-vacancy" ~}}
      <a href="https://goodmoves.com/vacancy/{{Id}}/{{slugify title}}">{{{truncate (trim title) 140}}}</a>
      {{else}}
      {{{truncate (trim title) 140}}}
      {{/compare ~}}
      {{/ifAny ~}}
    </div>
  </div>
  {{#if description ~}}
  <div class="card-body">
    {{{truncate (striptags description) 280}}}
  </div>
  {{/if ~}}
  {{#ifAny path url ~}}
  <div class="card-footer">
    <a href="{{default path url}}" class="btn btn-link">Read more</a>
  </div>
  {{else}}
  {{#compare ../_type "goodmoves-vacancy" ~}}
  <div class="card-footer">
    <a href="https://goodmoves.com/vacancy/{{Id}}/{{slugify title}}" class="btn btn-link">Read more</a>
  </div>
  {{/compare ~}}
  {{/ifAny ~}}
  {{/with ~}}
</div>
{{/each ~}}

{{> pagination paging=@root.data.primaryResponse.pagination}}