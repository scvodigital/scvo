{{#if @root.data.primaryResponse.hits.hits.[0] ~}}
<div class="container">
  {{> breadcrumbs_cms_page}}
</div>

<div class="bg-primary events-warning">
  <div class="container b-padding">
    <div class="v-margin">
      <h3>Access to your Credit Union Account</h3>
      <p>
        In light of the ongoing situation with coronavirus (COVID-19), we would like to remind members about how they can transact with SCVO Credit union.
      </p>
      <p>
        You may wish to:
      </p>
      <ul>
        <li>Speak to a member of staff, if they are unavailable leave a voicemail  – 0131 474 8003</li>
        <li>Submit a query via <a href="mailto:credit.union@scvo.org.uk">email</a></li>
        <li>Use our <a href="https://members.scvocreditunion.scot">online banking</a></li>
      </ul>
    </div>
  </div>
</div>

{{#with @root.data.primaryResponse.hits.hits.[0]._source ~}}
{{#if header_background ~}}
<div class="bg-header b-margin-full" style="background-image:url('{{header_background}}');">
  <div class="t-padding-full{{#unless hide_overlay}} overlay{{/unless}}">
    <div class="container">
      {{#if header_text ~}}
      <img src="{{header_text}}" alt="{{{title}}}" class="v-padding-full" />
      {{else}}
      <h1 class="v-padding-full">{{{title}}}</h1>
      {{/if ~}}
    </div>
  </div>
</div>
{{else}}
<div class="container">
  <h1>{{{title}}}</h1>
</div>
{{/if ~}}
{{/with ~}}
{{/if ~}}