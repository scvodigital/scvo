{{#compare @root.data.postsLatest.hits.total ">" 0 ~}}
<div class="related-posts v-padding-full bg-gray">
  <div class="container b-margin-full">
    {{#compare @root.data.postsLatest.hits.total ">" 0 ~}}
    <h3 class="b-margin">
      Latest posts&ensp;
      <a href="{{@root.request.url.pathname}}/posts" class="caption">See more</a>
    </h3>
    <div class="columns">
      {{#each (split "0,1,2,3" ",") ~}}
      {{#with (getProperty @root.data.postsLatest.hits.hits (concat this '._source')) ~}}
      <div class="column col-3 col-lg-6 col-sm-12">
        <div class="columns">
          <div class="column col-3 col-md-4">
            <a href="/{{replace post_slug '_' '/'}}" class="pull-left r-margin no-underline"><img src="{{default og_image author_avatar}}" alt="{{{author_name_full}}}" class="avatar avatar-lg" /></a>
          </div>
          <div class="column col-9 col-md-8">
            <span class="caption">{{momentFormat (moment publish_on 'x') "Do MMMM YYYY"}}</span><br />
            <a href="/{{replace post_slug '_' '/'}}">{{{trim title}}}</a>
          </div>
        </div>
      </div>
      {{/with ~}}
      {{/each ~}}
    </div>
    {{/compare ~}}
  </div>
</div>
{{/compare ~}}